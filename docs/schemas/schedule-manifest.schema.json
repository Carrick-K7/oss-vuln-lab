{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://oss-vuln-digger.local/schemas/schedule-manifest.schema.json",
  "title": "oss-vuln-digger Schedule Manifest",
  "type": "object",
  "required": ["name", "tasks"],
  "properties": {
    "schema_version": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "tasks": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/task"
      }
    }
  },
  "additionalProperties": true,
  "$defs": {
    "task": {
      "type": "object",
      "required": ["name", "every_minutes", "job"],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "every_minutes": {
          "type": "integer",
          "minimum": 1
        },
        "enabled": {
          "type": "boolean",
          "default": true
        },
        "job": {
          "$ref": "#/$defs/job"
        }
      },
      "additionalProperties": true
    },
    "job": {
      "type": "object",
      "required": ["name", "mode", "target"],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "mode": {
          "type": "string",
          "enum": ["scan", "replay_cve", "replay_manifest"]
        },
        "target": {
          "type": "string",
          "minLength": 1
        },
        "cve_id": {
          "type": "string",
          "minLength": 1
        },
        "manifest": {
          "type": "string",
          "minLength": 1
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "mode": {
                "const": "replay_cve"
              }
            },
            "required": ["mode"]
          },
          "then": {
            "required": ["cve_id"]
          }
        },
        {
          "if": {
            "properties": {
              "mode": {
                "const": "replay_manifest"
              }
            },
            "required": ["mode"]
          },
          "then": {
            "required": ["manifest"]
          }
        }
      ],
      "additionalProperties": true
    }
  }
}
